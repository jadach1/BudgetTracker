<script>
    function submitListOfExpenses() {
        if (listOfExpensesCounter > 0) {
            const form = document.getElementById("expenseForm")
            const arrayOfFormData = [];
            @* listOfExpenses.forEach(expense => {
                ajaxCall(expense);
            }) *@
                ajaxCallToExpenseController("expense");
        }
    }

         function ajaxCallToExpenseController(expense) {
            const obj = JSON.stringify(listOfExpenses);
            console.log(expense);
            console.log(obj);
            $.ajax({

                // Our sample url to make request 
                url:
                    '/Expenses/Create',

                // Type of Request
                contentType: 'application/json; charset=utf-8',
                type: "Post",
                data: obj,

                // Function to call when to
                // request is ok 
                success: function (data) {
                    let x = JSON.stringify(data);
                    console.log(x);
                     //ajaxCallSaveExpenses();
                     ClearExpenseListItems();
                },

                // Error handling 
                error: function (error) {
                    //console.log(`Error ${error}`);
                    console.log(error)
                }
            });
        }

        function ajaxCallSaveExpenses() {
            $.ajax({

                // Our sample url to make request 
                url:
                    '/Expenses/CreateSave',

                // Type of Request
                type: "Post",

                // Function to call when to request is ok 
                success: function (data) {
                    let x = JSON.stringify(data);
                    console.log(x);
                    //clear all items
                    
                },

                // Error handling 
                error: function (error) {
                    console.log(`Error2 ${error}`);
                    console.log(error)
                }
            });
        }

</script>